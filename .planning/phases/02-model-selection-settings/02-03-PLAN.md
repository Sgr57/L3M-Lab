---
phase: 02-model-selection-settings
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified: []
autonomous: false
requirements: [MSEL-01, MSEL-02, MSEL-03, MSEL-04, MSEL-05, MSEL-06, MSEL-07, MSEL-08, STNV-02, STNV-03, STNV-04]

must_haves:
  truths:
    - "All phase 2 success criteria verified by human interaction with the running app"
    - "Search, chips, accordion, settings all function correctly in browser"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of the complete Phase 2 implementation by running the app and testing all user-facing features.

Purpose: Confirm that the refactored ModelSelector and verified Settings page work correctly in a real browser session -- catching rendering issues, interaction bugs, and API integration problems that type-checking alone cannot detect.
Output: Human confirmation that all Phase 2 success criteria are met.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-model-selection-settings/02-CONTEXT.md
@.planning/phases/02-model-selection-settings/02-01-SUMMARY.md
@.planning/phases/02-model-selection-settings/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and run build verification</name>
  <files></files>
  <read_first>
    - .planning/phases/02-model-selection-settings/02-02-SUMMARY.md (confirm Plan 02 completed successfully)
  </read_first>
  <action>
Run the full verification suite before handing to human:

1. Run `npm run build` to confirm production build passes with zero errors
2. Run `npm run lint` to confirm no linting issues
3. Start the dev server with `npm run dev` (leave running in background)
4. Report the local URL (typically http://localhost:5173) for human verification
  </action>
  <verify>
    <automated>cd /Users/emanuele/Projects/CompareLocalLLM && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - `npm run lint` exits with code 0
    - Dev server is running and accessible
  </acceptance_criteria>
  <done>Build and lint pass. Dev server running for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete Phase 2 UI</name>
  <files></files>
  <action>
Human verifies the complete Phase 2 implementation in the browser.

What was built:
- Two-row local model chips (name+selectors+trash / size+cache dot+backend badge)
- Cloud models accordion (closed by default, 6 models across 3 providers, custom model ID input)
- Cloud chips with dashed purple border and provider badge
- Settings page with API keys, show/hide toggle, test connection

How to verify -- open the dev server URL (http://localhost:5173) in Chrome:

1. Search and add a local model (MSEL-01, MSEL-02, MSEL-04, MSEL-05):
   - Type "qwen" in the search box
   - Verify autocomplete shows results with: model ID, pipeline tag, ONNX badge, transformers.js badge (if applicable), download count, likes count
   - Click a result (e.g., "onnx-community/Qwen2.5-0.5B-Instruct")
   - Verify the chip appears with TWO rows:
     - Row 1: model name, quantization dropdown, backend dropdown, trash icon (SVG bin, not "x")
     - Row 2: estimated download size (e.g., "~250 MB"), green or gray cache dot, color-coded backend badge

2. Change quantization and verify recalculation (MSEL-05, D-13):
   - Change the quantization dropdown on the chip
   - Verify the download size estimate changes
   - Verify the cache dot may change color (gray if not cached for new quant)

3. A/B comparison (MSEL-06):
   - Search and add the SAME model again
   - Change backend or quantization on the second chip
   - Verify both chips coexist with different configs

4. Cloud models accordion (MSEL-07, D-07, D-08, D-09):
   - If no API keys configured: go to Settings first (step 5), then return
   - Click the "Cloud Models" accordion header
   - Verify it expands showing providers grouped with 2 models each
   - Click a cloud model quick-add button
   - Verify it appears as a chip ABOVE the accordion with: dashed purple border, model name, purple provider badge, trash icon
   - Click "+ Custom" for a provider, type a model name, press Enter
   - Verify it is added as a chip

5. Settings page (STNV-02, STNV-03, STNV-04):
   - Navigate to /settings
   - Verify 3 provider cards (OpenAI, Anthropic, Google)
   - Type an API key, click Show/Hide toggle, verify it works
   - Click Test button (if you have a valid key), verify connection result shows

6. Visual checks (MSEL-08):
   - Verify cloud chips have dashed border (not solid) with purple color
   - Verify cloud chips have purple provider badge
   - Verify local chips have solid border
   - Verify local chip backend badge uses correct colors (blue for WebGPU, green for WASM)
  </action>
  <verify>
    <automated>MISSING -- human-verify checkpoint, requires visual browser inspection</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms all 6 verification sections pass
    - No visual rendering bugs reported
    - No interaction bugs reported (selectors, accordion, trash buttons, custom input)
  </acceptance_criteria>
  <done>Human approves all visual and functional checks covering MSEL-01 through MSEL-08 and STNV-02 through STNV-04. Type "approved" or describe issues.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Verification-only plan, no new code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | No new code in this plan -- verification only |
</threat_model>

<verification>
- npm run build passes
- npm run lint passes
- Human confirms all 6 verification sections pass
</verification>

<success_criteria>
- Production build compiles with zero errors
- Lint passes with zero issues
- Human approves all visual and functional checks covering MSEL-01 through MSEL-08 and STNV-02 through STNV-04
</success_criteria>

<output>
After completion, create `.planning/phases/02-model-selection-settings/02-03-SUMMARY.md`
</output>
