---
phase: 01-foundation-critical-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vite.config.ts
autonomous: true
requirements:
  - FNDN-01
  - FNDN-02
  - FNDN-04
  - STNV-01

must_haves:
  truths:
    - "App runs in dev with crossOriginIsolated === true in browser console"
    - "SharedArrayBuffer is available (typeof SharedArrayBuffer !== 'undefined')"
    - "Worker bundle uses ES module format in production build"
    - "NavBar displays 'CompareLocalLLM' logo, Compare and Settings links, and a WebGPU badge"
  artifacts:
    - path: "vite.config.ts"
      provides: "COOP/COEP headers and worker format configuration"
      contains: "Cross-Origin-Opener-Policy"
  key_links:
    - from: "vite.config.ts"
      to: "browser crossOriginIsolated flag"
      via: "HTTP response headers on index.html"
      pattern: "Cross-Origin-Opener-Policy.*same-origin"
    - from: "vite.config.ts"
      to: "src/workers/inference.worker.ts"
      via: "worker.format: 'es' matching type: 'module' in workerBridge.ts"
      pattern: "format.*es"
---

<objective>
Add cross-origin isolation headers and worker format declaration to Vite config, and verify existing NavBar/WebGPU badge satisfy FNDN-04 and STNV-01.

Purpose: Enable SharedArrayBuffer for multi-threaded WASM execution (accurate benchmarks) and ensure worker builds use ES module format matching the runtime `type: 'module'` instantiation. Also confirm already-implemented NavBar and WebGPU badge meet requirements.

Output: Updated vite.config.ts with COOP/COEP headers (server + preview) and worker format 'es'.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-critical-fixes/01-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From vite.config.ts (CURRENT STATE - will be modified):
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
  optimizeDeps: {
    exclude: ['@huggingface/transformers'],
  },
})
```

From src/lib/workerBridge.ts (line 11, NOT modified, context only):
```typescript
worker = new Worker(new URL('../workers/inference.worker.ts', import.meta.url), {
  type: 'module',
})
```

From src/components/NavBar/index.tsx (NOT modified, verify only):
```typescript
export function NavBar() {
  // Renders: logo "CompareLocalLLM", Compare link, Settings link, WebGPU badge
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add COOP/COEP headers and worker format to Vite config</name>
  <files>vite.config.ts</files>
  <read_first>
    - vite.config.ts (current state to modify)
    - src/lib/workerBridge.ts (verify worker is created with type: 'module' on line 11)
    - .planning/phases/01-foundation-critical-fixes/01-RESEARCH.md (Pattern 1 and Pattern 3 sections for exact config)
  </read_first>
  <action>
Replace the entire vite.config.ts with the following configuration. This implements D-04 (COOP/COEP headers) and D-05 (worker format 'es').

The exact target state of vite.config.ts:

```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

const crossOriginHeaders = {
  'Cross-Origin-Opener-Policy': 'same-origin',
  'Cross-Origin-Embedder-Policy': 'require-corp',
}

export default defineConfig({
  plugins: [react(), tailwindcss()],
  optimizeDeps: {
    exclude: ['@huggingface/transformers'],
  },
  server: {
    headers: crossOriginHeaders,
  },
  preview: {
    headers: crossOriginHeaders,
  },
  worker: {
    format: 'es',
  },
})
```

Key details:
- `crossOriginHeaders` extracted as a const shared between `server.headers` and `preview.headers` (avoids duplication, per Research Pitfall 4)
- `server.headers` applies to `vite dev` server
- `preview.headers` applies to `vite preview` server (Research Pitfall 4 warns this needs separate config)
- `worker: { format: 'es' }` ensures production worker bundle is ES module format, matching the `type: 'module'` in workerBridge.ts line 11
- Existing `plugins` and `optimizeDeps.exclude` remain unchanged
  </action>
  <verify>
    <automated>cd /Users/emanuele/Projects/CompareLocalLLM && grep -q "Cross-Origin-Opener-Policy" vite.config.ts && grep -q "Cross-Origin-Embedder-Policy" vite.config.ts && grep -q "format: 'es'" vite.config.ts && grep -q "preview:" vite.config.ts && echo "PASS: All config entries present" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - vite.config.ts contains the string `'Cross-Origin-Opener-Policy': 'same-origin'`
    - vite.config.ts contains the string `'Cross-Origin-Embedder-Policy': 'require-corp'`
    - vite.config.ts contains `server:` block with `headers: crossOriginHeaders`
    - vite.config.ts contains `preview:` block with `headers: crossOriginHeaders`
    - vite.config.ts contains `worker:` block with `format: 'es'`
    - vite.config.ts still contains `exclude: ['@huggingface/transformers']`
    - vite.config.ts still contains `plugins: [react(), tailwindcss()]`
    - `npx tsc --noEmit` passes (no type errors introduced)
  </acceptance_criteria>
  <done>Vite config updated with COOP/COEP headers for both dev and preview servers, and worker format set to 'es'. TypeScript compilation passes.</done>
</task>

<task type="auto">
  <name>Task 2: Verify existing NavBar and WebGPU badge satisfy FNDN-04 and STNV-01</name>
  <files>src/components/NavBar/index.tsx</files>
  <read_first>
    - src/components/NavBar/index.tsx (verify it has logo, links, badge)
    - src/lib/webgpuDetect.ts (verify detection utility exists)
    - src/hooks/useWebGPU.ts (verify hook exists)
    - src/stores/useSettingsStore.ts (verify webgpuSupported state)
  </read_first>
  <action>
Verify (read-only, no modifications expected) that the existing NavBar implementation satisfies:

1. FNDN-04: WebGPU badge shows supported/unsupported based on runtime detection
   - NavBar reads `webgpuSupported` from `useSettingsStore`
   - Conditionally renders "WebGPU supported" (green) or "WebGPU not available" (gray)
   - Detection chain: webgpuDetect.ts -> useWebGPU.ts hook -> useSettingsStore.webgpuSupported -> NavBar

2. STNV-01: NavBar displays logo "CompareLocalLLM", Compare link (active), Settings link, WebGPU badge
   - Logo text: "CompareLocalLLM" in `span` with font-bold
   - Compare link: NavLink to "/" with isActive styling
   - Settings link: NavLink to "/settings" with isActive styling
   - WebGPU badge: conditional span based on webgpuSupported

If all checks pass, no code changes are needed. The task is a verification-only pass.

If any check fails (missing text, broken link, missing badge), fix the specific issue in NavBar/index.tsx. This is NOT expected based on code review -- the implementation is complete.
  </action>
  <verify>
    <automated>cd /Users/emanuele/Projects/CompareLocalLLM && grep -q "CompareLocalLLM" src/components/NavBar/index.tsx && grep -q 'to="/"' src/components/NavBar/index.tsx && grep -q 'to="/settings"' src/components/NavBar/index.tsx && grep -q "webgpuSupported" src/components/NavBar/index.tsx && grep -q "WebGPU supported" src/components/NavBar/index.tsx && grep -q "WebGPU not available" src/components/NavBar/index.tsx && echo "PASS: NavBar satisfies FNDN-04 and STNV-01" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/NavBar/index.tsx contains the string "CompareLocalLLM" (logo text)
    - src/components/NavBar/index.tsx contains `to="/"` (Compare link)
    - src/components/NavBar/index.tsx contains `to="/settings"` (Settings link)
    - src/components/NavBar/index.tsx contains `webgpuSupported` (reads detection state)
    - src/components/NavBar/index.tsx contains "WebGPU supported" (positive badge text)
    - src/components/NavBar/index.tsx contains "WebGPU not available" (negative badge text)
    - src/hooks/useWebGPU.ts exists (detection hook)
    - src/lib/webgpuDetect.ts exists (detection utility)
  </acceptance_criteria>
  <done>NavBar confirmed to display app name, navigation links, and WebGPU support badge. FNDN-04 and STNV-01 are satisfied by existing implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser <-> CDN | HuggingFace model downloads cross COEP boundary |
| Dev server <-> Browser | COOP/COEP headers set on HTTP responses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | SharedArrayBuffer / Spectre | mitigate | COOP/COEP headers enable cross-origin isolation, which IS the browser's Spectre mitigation. This plan ADDS security. |
| T-01-02 | Denial of Service | COEP blocking external resources | accept | POC has no external cross-origin resources in dev mode (Tailwind bundled, HF downloads via worker fetch). If resources break, switch COEP to `credentialless`. Low risk per Research A1/A2. |
</threat_model>

<verification>
1. Run `npx tsc --noEmit` -- no type errors
2. Run `npx vite build` -- build succeeds
3. Start dev server, open browser console, verify `crossOriginIsolated === true`
4. Verify NavBar renders logo, Compare link, Settings link, WebGPU badge
</verification>

<success_criteria>
- vite.config.ts contains COOP/COEP headers for server and preview
- vite.config.ts contains worker format 'es'
- TypeScript compilation succeeds
- Build succeeds
- NavBar displays CompareLocalLLM logo, Compare/Settings links, WebGPU badge
- FNDN-01, FNDN-02, FNDN-04, STNV-01 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-critical-fixes/01-01-SUMMARY.md`
</output>
