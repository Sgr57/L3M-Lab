---
phase: 06-cleanup-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "REQUIREMENTS.md traceability table shows [x] Complete for all 43 satisfied requirements"
    - "REQUIREMENTS.md traceability table shows MSEL-03 as Descoped (not Pending)"
    - "REQUIREMENTS.md v1 requirement list marks MSEL-03 with Descoped annotation"
    - "ROADMAP.md Phase 2 Requirements line does not contain MSEL-03"
    - "ROADMAP.md progress table shows all 6 phases with correct completion status"
    - "REQUIREMENTS.md coverage count reflects 43 satisfied + 1 descoped = 44 total"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate traceability reflecting actual project completion state"
      contains: "[x]"
    - path: ".planning/ROADMAP.md"
      provides: "Accurate phase progress and success criteria"
      contains: "Complete"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "requirement-to-phase mapping"
      pattern: "Phase [1-5].*Complete"
---

<objective>
Update tracking documentation to reflect actual project completion state.

Purpose: Close tech debt items 3 and 4 from v1.0-MILESTONE-AUDIT.md -- REQUIREMENTS.md traceability checkboxes stuck at Pending, and MSEL-03 descope not reflected in documentation.
Output: Accurate REQUIREMENTS.md and ROADMAP.md reflecting all 43 satisfied requirements, 1 descoped requirement, and 5 completed phases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md traceability and MSEL-03 status</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file -- see traceability table at lines 117-163, coverage summary at lines 164-167)
    - .planning/v1.0-MILESTONE-AUDIT.md (audit confirms 43/44 satisfied, MSEL-03 descoped per D-03)
  </read_first>
  <action>
In `.planning/REQUIREMENTS.md`, make these exact changes:

**1. Mark MSEL-03 as descoped in the v1 requirements list (line 21):**
FROM: `- [ ] **MSEL-03**: User can paste a direct model ID (containing `/`) and add it via smart detection in dropdown or Enter key with API validation`
TO:   `- [~] **MSEL-03**: ~~User can paste a direct model ID (containing `/`) and add it via smart detection in dropdown or Enter key with API validation~~ *(Descoped per D-03: existing filtered search is sufficient for POC)*`

**2. Update all 43 satisfied requirement checkboxes from `[ ]` to `[x]` in the v1 requirements list (lines 12-68):**
Change every `- [ ] **FNDN-` to `- [x] **FNDN-`
Change every `- [ ] **MSEL-` to `- [x] **MSEL-` (EXCEPT MSEL-03 which uses `[~]`)
Change every `- [ ] **PRMT-` to `- [x] **PRMT-`
Change every `- [ ] **CTRL-` to `- [x] **CTRL-`
Change every `- [ ] **EXEC-` to `- [x] **EXEC-`
Change every `- [ ] **RSLT-` to `- [x] **RSLT-`
Change every `- [ ] **EXPT-` to `- [x] **EXPT-`
Change every `- [ ] **STNV-` to `- [x] **STNV-`

**3. Update the traceability table (lines 117-162):**
Change every `| Pending |` to `| Complete |` EXCEPT for MSEL-03.
For MSEL-03 specifically (line 125):
FROM: `| MSEL-03 | Phase 2 | Pending |`
TO:   `| MSEL-03 | Phase 2 | Descoped (D-03) |`

**4. Update the coverage summary (lines 164-167):**
FROM:
```
**Coverage:**
- v1 requirements: 44 total
- Mapped to phases: 44
- Unmapped: 0
```
TO:
```
**Coverage:**
- v1 requirements: 44 total
- Satisfied: 43
- Descoped: 1 (MSEL-03)
- Unmapped: 0
```

**5. Update the last-updated date (line 171):**
FROM: `*Last updated: 2026-04-10 after roadmap creation*`
TO:   `*Last updated: 2026-04-12 after v1.0 milestone audit closure*`
  </action>
  <verify>
    <automated>cd /Users/emanuele/Projects/CompareLocalLLM && grep -c "\[x\]" .planning/REQUIREMENTS.md && grep "MSEL-03" .planning/REQUIREMENTS.md | head -3 && grep "Descoped" .planning/REQUIREMENTS.md | head -3 && grep "Satisfied: 43" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\[x\]" .planning/REQUIREMENTS.md` returns 43 (all satisfied requirements checked)
    - `grep -c "\[~\]" .planning/REQUIREMENTS.md` returns 1 (MSEL-03 only)
    - `grep "MSEL-03.*Descoped" .planning/REQUIREMENTS.md` returns at least 1 match in the traceability table
    - `grep "Satisfied: 43" .planning/REQUIREMENTS.md` returns 1 match
    - `grep "Descoped: 1" .planning/REQUIREMENTS.md` returns 1 match
    - `grep -c "Pending" .planning/REQUIREMENTS.md` returns 0 (no more Pending statuses)
    - `grep "2026-04-12" .planning/REQUIREMENTS.md` returns 1 match (updated date)
  </acceptance_criteria>
  <done>All 43 satisfied requirements marked [x] Complete. MSEL-03 marked [~] Descoped with rationale. Traceability table and coverage summary accurate.</done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md phase progress and MSEL-03 cleanup</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (full file -- see Phase 2 at lines 40-53, Phase 6 at lines 108-118, progress table at lines 125-132)
    - .planning/v1.0-MILESTONE-AUDIT.md (confirms MSEL-03 descoped, all 5 phases complete)
  </read_first>
  <action>
In `.planning/ROADMAP.md`, make these exact changes:

**1. Verify Phase 2 Requirements line (line 42) does NOT include MSEL-03:**
Current: `**Requirements**: MSEL-01, MSEL-02, MSEL-04, MSEL-05, MSEL-06, MSEL-07, MSEL-08, STNV-02, STNV-03, STNV-04`
This is already correct (MSEL-03 absent). Do NOT modify this line.

**2. Update Phase 2 SC #2 (line 45) to clarify it does not cover MSEL-03:**
The current SC #2 text is fine ("Selected models appear as chips with working quantization selector..."). No change needed IF the audit's concern was only about the Requirements line. Since the Requirements line already omits MSEL-03, add a note after the Phase 2 success criteria block to document the descope:

After line 47 (the last SC line `  4. User can configure API keys...`), add:
```
**Note**: MSEL-03 (direct model ID paste) was descoped per D-03 during Phase 2 planning. See v1.0-MILESTONE-AUDIT.md.
```

**3. Update the phase checklist (lines 15-20) to mark completed phases:**
FROM:
```
- [ ] **Phase 1: Foundation & Critical Fixes** - COOP/COEP headers, accurate token counting, WebGPU badge, app shell with color system
- [ ] **Phase 2: Model Selection & Settings** - HuggingFace search, model chips with config, cloud model quick-add, API key management
- [ ] **Phase 3: Prompt Input & Test Controls** - Multi-line prompt, generation parameters, pre-download, run/cancel buttons
- [ ] **Phase 4: Execution Engine & Progress** - Hardened sequential execution, streaming output, live progress, GPU memory safety, cloud API calls
- [ ] **Phase 5: Results & Export** - Summary stats, performance charts, comparison table, output cards, JSON/CSV/Markdown export
- [ ] **Phase 6: Cleanup & Documentation** - Remove dead code, fix visual inconsistency, update tracking docs
```
TO:
```
- [x] **Phase 1: Foundation & Critical Fixes** - COOP/COEP headers, accurate token counting, WebGPU badge, app shell with color system
- [x] **Phase 2: Model Selection & Settings** - HuggingFace search, model chips with config, cloud model quick-add, API key management
- [x] **Phase 3: Prompt Input & Test Controls** - Multi-line prompt, generation parameters, pre-download, run/cancel buttons
- [x] **Phase 4: Execution Engine & Progress** - Hardened sequential execution, streaming output, live progress, GPU memory safety, cloud API calls
- [x] **Phase 5: Results & Export** - Summary stats, performance charts, comparison table, output cards, JSON/CSV/Markdown export
- [ ] **Phase 6: Cleanup & Documentation** - Remove dead code, fix visual inconsistency, update tracking docs
```
(Phase 6 remains `[ ]` since it is currently executing.)

**4. Update the progress table (lines 125-132):**
Ensure Phase 6 row shows correct plan count once planning is done. Update:
FROM: `| 6. Cleanup & Documentation | 0/0 | Planned | - |`
TO:   `| 6. Cleanup & Documentation | 0/2 | Executing | - |`

**5. Update Phase 6 Plans line (line 118):**
FROM: `**Plans**: TBD`
TO:   `**Plans**: 2 plans`

(The plan list lines will be added by the commit step -- the orchestrator handles that.)
  </action>
  <verify>
    <automated>cd /Users/emanuele/Projects/CompareLocalLLM && grep -c "\[x\] \*\*Phase" .planning/ROADMAP.md && grep "Phase 6.*Cleanup" .planning/ROADMAP.md | head -2 && grep "MSEL-03" .planning/ROADMAP.md | head -3 && grep "Plans.*2 plans" .planning/ROADMAP.md | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\[x\] \*\*Phase" .planning/ROADMAP.md` returns 5 (phases 1-5 checked)
    - `grep "\[ \] \*\*Phase 6" .planning/ROADMAP.md` returns 1 match (phase 6 still unchecked)
    - `grep "MSEL-03.*descoped\|MSEL-03.*D-03" .planning/ROADMAP.md` returns at least 1 match (descope note present)
    - MSEL-03 does NOT appear in Phase 2 Requirements line (already verified: `grep "Requirements.*MSEL-03" .planning/ROADMAP.md` returns 0)
    - `grep "0/2.*Executing" .planning/ROADMAP.md` returns 1 match (Phase 6 progress updated)
    - `grep "Plans.*2 plans" .planning/ROADMAP.md` matches at least the Phase 6 entry
  </acceptance_criteria>
  <done>ROADMAP.md phases 1-5 marked complete. MSEL-03 descope documented. Phase 6 plan count and progress updated. Documentation accurately reflects project state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries. This plan modifies only planning documentation files (Markdown). No code, no user input, no data flow changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | T (Tampering) | Documentation files | accept | Planning docs are developer-facing only, stored in git with full history. No security impact from documentation updates. |
</threat_model>

<verification>
1. `grep -c "Pending" .planning/REQUIREMENTS.md` returns 0
2. `grep -c "\[x\]" .planning/REQUIREMENTS.md` returns 43
3. `grep -c "\[x\] \*\*Phase" .planning/ROADMAP.md` returns 5
4. Phase 2 Requirements line in ROADMAP.md does not contain "MSEL-03"
5. MSEL-03 descope is documented in both REQUIREMENTS.md and ROADMAP.md
</verification>

<success_criteria>
- REQUIREMENTS.md traceability reflects 43 Complete + 1 Descoped requirements
- ROADMAP.md phases 1-5 marked complete, Phase 6 shows 2 plans executing
- MSEL-03 descope documented with D-03 reference in both files
- No "Pending" status remains in REQUIREMENTS.md traceability table
</success_criteria>

<output>
After completion, create `.planning/phases/06-cleanup-documentation/06-02-SUMMARY.md`
</output>
